# x3coding-challenge

```sh
cp .env.example .env
php artisan key:generate
chmod -R 775 storage bootstrap/cache
```

composer install
```sh
composer install
```

migrations
```sh
php artisan migrate
```
